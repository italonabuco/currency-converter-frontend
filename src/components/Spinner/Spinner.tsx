import React from 'react';

// mui
import { CircularProgress } from '@material-ui/core';

const Spinner = () => {
  return <CircularProgress color="primary" size={48} thickness={3} />;
};

export default Spinner;
